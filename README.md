# ticker — 終端機行情面板

在 Windows Terminal 裡跑的即時行情面板：上方看台股、美股、日韓、總經與類股，右側看選中個股的日 K 與型態判讀，下方留給 Claude Code。

```
  加權     S&P     Nasdaq    道瓊     費半    日經    KOSPI   台指期   電子期
 47,978  7,706.03  26,936   51,511   12,534  65,796  7,080.92 48,110  3,065.00
    觀察    台股    美股    日韓    總經    類股   · 0s          │ 上詮 3363.TWO  643.00  -2.58%  日K 42 根
 名稱        代號      今日區間   量比  乖離  年區間  外資   價格   漲跌   幅度  型態   │  ▄ █ ▄ ▄ ▄ ...
 元大高股息  0056.TW   ──●────    1.2  +2.3%  98%   +3日  56.60  -0.55 -0.96% 多頭排列 │ MA5 660  MA20 704  MA60 619
 國巨        2327.TW   ────●──    0.8  +3.1%  42%         573.00 +2.00 +0.35% 長上影線 │ 長上影線 盤中衝高被賣回，上方有賣壓
```

## 功能

**報價**
- 多頁籤：庫存、觀察、台股、美股、日韓、總經、類股；依窗格高度自動分頁，每 20 秒自動翻頁。
- 台股、加權指數走證交所 MIS，台指期、電子期走期交所，獨立 5 秒迴圈，不跟國際報價排隊。
- 設了富果 API key 時，選中的那一檔改用富果 WebSocket 逐筆推播（免費方案一次一檔）；富果沒推時 MIS 接手。
- 國際報價走 Yahoo（約 15 秒一輪），被限流時改抓 CNBC 補上對得到代號的部分。
- 收盤的市場跳過不抓，夜裡請求量大幅減少。

**表格欄位**
- 今日區間條（依價量上漸層色）、量比（爆量黃底、量縮變暗）。
- 乖離：現價對 20 日均線，超過 ±8% 標追高／回檔。
- 年區間：現價在 52 週高低點的位置。
- 外資：證交所 T86 外資連續買賣超天數（上市股）。
- 型態：自動判讀最重要的一條（見下方）。
- 庫存頁的漲跌、幅度改成今日損益與總報酬。
- 報價跳動時價格格閃一下；換頁用翻牌動畫。

**右側 K 線面板**
- 選中個股的日 K（最近 90 根），K 棒顏色代表站在哪條均線上：亮紅站上季線、暗紅只站上月線、綠色跌破月線。
- 今天那根用即時報價組成，盤中跟著跳，不用等盤後。
- 下方列 MA5／MA20／MA60、區間高低，以及最多兩條型態的白話說明。
- 視窗太窄時自動收起。

**型態判讀**（用已收盤的日 K，少見的優先列）

| 類別 | 型態 |
|---|---|
| K 棒 | 多頭吞噬、空頭吞噬、十字線、長下影線、長上影線 |
| 位置 | 突破月高、跌破月低、回測月線 |
| 趨勢 | 多頭排列、空頭排列 |

**其他**
- 底部跑馬燈：異動排行、警示訊息，左側固定 DXY、Brent、黃金、VIX、美債 10Y。
- 警示：`portfolio.json` 的「警示」設到價上下限，另有爆量警示，每條每天只響一次。
- 觀察清單可依產業分組，組名當成一列標題。
- 單一實例鎖（port 47653），不會重複開出兩個面板。

## 操作

| 按鍵 | 動作 |
|---|---|
| `↑` `↓` | 切換選中個股，右側 K 線跟著換（自動翻頁重新計時） |
| `←` `→`、滑鼠滾輪 | 上一頁／下一頁 |
| `1`–`9`、點頁籤 | 跳到該頁；再點同一頁籤翻它的下一子頁 |
| 點個股列 | 選中該檔 |
| `+` `-` | K 線縮放（三段） |
| 空白鍵 | 暫停／恢復自動翻頁（標題出現 ⏸） |
| `q` | 離開 |

## 安裝

```
git clone https://github.com/Devchou012/ticker.git %USERPROFILE%\ticker
python %USERPROFILE%\ticker\install.py
```

`install.py` 可以重複跑，已完成的步驟會跳過：
1. 安裝 `rich`、`yfinance`。
2. 從 `portfolio.example.json` 建立 `portfolio.json`（不進 git，各台機器自己一份）。
3. 加 Claude Code SessionStart hook：在 Windows Terminal 開 Claude Code 時自動在上方分割出面板。
4. 在 PowerShell profile 加 `cs`（別名 `股票介面`）指令。
5. 桌面建立「行情面板」捷徑。

**富果逐筆（選用）**

```
pip install fugle-marketdata
powershell -ExecutionPolicy Bypass -File "$HOME\ticker\set_fugle_key.ps1"
```

key 存成使用者環境變數 `FUGLE_API_KEY`，輸入時只顯示 `*`，不會進 git。沒設就用 MIS 5 秒快照。

## 管理清單

```
python watch.py 興富發 2881 NVDA   # 加入觀察（台股名稱或代號、美股代號）
python watch.py -d 興富發           # 移除
python watch.py                     # 列出
```

面板一秒內會讀到變更。庫存（股數、成本）與警示直接改 `portfolio.json`，格式見 `portfolio.example.json`。

## 檔案

| 檔案 | 用途 |
|---|---|
| `ticker.py` | 面板本體 |
| `watch.py` | 觀察清單增刪 |
| `install.py` | 新機器安裝 |
| `stocks.cmd` / `autostart.ps1` | 開新視窗／從 Claude Code 自動分割窗格 |
| `set_fugle_key.ps1` | 存富果 API key |
| `portfolio.json` | 個人清單（不進 git） |
| `daily.json` / `chips.json` | 日線與外資籌碼快取（不進 git，自動產生） |
| `test_*.py` | 自我檢查，不連網：`for %f in (test_*.py) do python %f` |

`python ticker.py --once` 會抓一輪、把每一頁印出來後結束，可用來檢查資料來源。

## 已知限制

- 台股免費來源的極限是 MIS 5 秒快照；逐筆要富果（免費一次一檔）或券商 API（富邦 Neo API 需證券戶與憑證）。
- 交易時段寫死，不管國定假日與美股夏令時間，量比在這些時段會略偏。
- 期交所日盤收盤（13:45）到夜盤開盤（15:00）之間剛開面板，台指期會暫時空白。
- 日線每天盤後抓一次；盤中只補抓清單裡缺的個股（每 10 分鐘最多一次）。

## 變更紀錄

**2026-09-24**
- 富果 WebSocket 逐筆串流選中個股；今天的 K 棒用即時價組成，盤中即時變動。
- 觀察清單依產業分組；Yahoo 限流時改用 CNBC；今日區間條改成價量漸層色。
- 型態判讀：表格新增「型態」欄，K 線下方兩行白話說明。
- 方向鍵選股時自動翻頁重新計時。
- 盤中自動補抓缺日線的個股，不再等到盤後；上市未滿 20 天也照畫 K 線。
- 台股與台指期拆成獨立 5 秒迴圈，更新從約 20 秒縮到 5 秒。

**2026-09-23**
- 右側日 K 面板：點選或方向鍵選股、三段縮放、K 棒依均線上色；移除走勢圖讓面板更順。
- 新增乖離、52 週位置、外資連續買賣超、類股強弱、到價與爆量警示。
- 欄位收緊靠左，面板佔窗格 45%。

**2026-09-21**
- 觀察清單依產業分組，擴充美股、日股、總經。
- 報價跳動時閃爍價格格，翻牌改灰色。
- 底部跑馬燈，開盤中的市場排前面，並修正捲動不均。

**2026-09-20**
- 同步輸出（synchronized output）消除閃爍、底列不上色避免抖動。
- 修正 virtualenv 與中文使用者目錄下的安裝、libcurl 憑證路徑。
- 函式庫錯誤訊息改寫到 `ticker-errors.log`，不再噴到窗格。
- 新增代幣化黃金、24 小時白銀與銅。
- 跳過收盤市場、限制 log 大小、空白鍵暫停翻頁。
- `portfolio.json` 移出 git。

**2026-09-18**
- 初版：終端機行情面板與安裝程式。
- 台股與加權指數改用證交所 MIS 即時報價，不再用 Yahoo 延遲價。
- 擴充日韓、美股清單。
